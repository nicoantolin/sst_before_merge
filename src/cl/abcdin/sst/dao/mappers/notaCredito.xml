<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="notaCredito">
	
	<select id="getDiasNotaCredito" resultType="Integer">
		select 
			p.i_integer1
		from sstt_parametros p 
		where 
			p.s_nombre = 'dias nota credito'
	</select>
</mapper>